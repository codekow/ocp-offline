# OpenShift Configurations

Various OpenShift Configurations are located in this directory.

```sh
# apply yaml file
oc apply -f path/to/file

# apply kustomize configuration
# see https://kustomize.io/
oc apply -k path/to/kustomization.yaml
```
